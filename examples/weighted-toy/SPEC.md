# weighted-toy — spec

The minimal governing text `acceptance.toml`'s one weighted claim needs a real `clause_source`
(`spec/core.md` W2.3) to point at. This document is not a real specification in the sense the
format's other worked subject (`rs-verified-der/PROOF_MANIFEST.md`) carries; it exists only so the
weighted-toy certificate cites a real clause instead of an invented one.

## S-1

`slug(s)` never returns a string containing an uppercase ASCII letter.
